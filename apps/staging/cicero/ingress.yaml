apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cicero-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - cicero.staging.saltechng.com
      secretName: cicero-tls
  rules:
    - host: cicero.staging.saltechng.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cicero-n8n
                port:
                  number: 3008
